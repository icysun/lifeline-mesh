{
  "name": "lifeline-mesh",
  "version": "0.1.0",
  "description": "End-to-end encrypted emergency messaging. Offline-first. No server required.",
  "type": "module",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix crypto && npm install --prefix tools",
    "test": "npm run test:crypto && npm run test:vectors",
    "test:crypto": "npm --prefix crypto test",
    "test:vectors": "npm --prefix tools run validate-vectors",
    "generate:vectors": "npm --prefix tools run generate-vectors",
    "generate:sri": "npm --prefix tools run generate-sri",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "tsc --noEmit",
    "validate": "npm run lint && npm run typecheck && npm run test",
    "ci": "npm run validate",
    "prepare": "husky || true",
    "pre-commit": "lint-staged",
    "dev": "vite",
    "build": "vite build",
    "build:airgap": "node scripts/build-airgap.js",
    "preview": "vite preview",
    "test:integration": "node tests/integration/ble.test.js && node tests/integration/mesh-router.test.js",
    "test:unit": "npm run test",
    "test:offline-runtime": "node tests/integration/offline-runtime.test.js"
  },
  "keywords": [
    "cryptography",
    "nacl",
    "ed25519",
    "x25519",
    "disaster",
    "mesh",
    "emergency",
    "offline",
    "e2e-encryption"
  ],
  "author": "Lifeline Mesh Contributors",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/hiroshitanaka-creator/lifeline-mesh"
  },
  "dependencies": {
    "tweetnacl": "^1.0.3",
    "tweetnacl-util": "^0.15.1",
    "qrcode": "^1.5.3",
    "html5-qrcode": "^2.3.8"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.56.0",
    "@types/node": "^25.1.0",
    "eslint": "^8.56.0",
    "husky": "^9.0.0",
    "lint-staged": "^16.2.7",
    "typescript": "^5.3.0",
    "vite": "^5.4.0",
    "vite-plugin-singlefile": "^2.0.0"
  },
  "lint-staged": {
    "crypto/**/*.js": [
      "eslint --fix",
      "npm run test:crypto"
    ],
    "tools/**/*.js": [
      "eslint --fix"
    ],
    "*.{js,json}": [
      "eslint --fix"
    ]
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
